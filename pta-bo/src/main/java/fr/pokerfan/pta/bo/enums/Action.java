package fr.pokerfan.pta.bo.enums;

/**
 * Enum sur les actions possibles (call, fold et raise)
 * 
 * @author pierre.kerichard
 * 
 */
public enum Action {

	LIMP, CALL, FOLD, MIN_RAISE, RAISE, RE_RAISE;
}
